"use client";

const Navbar = () => {
  return (
    <div>
      <nav className="flex w-full h-16 shadow-md bg-white backdrop-blur-2xl fixed top-0 z-50">
        <div className="w-full flex justify-center items-center">
          <h1 className="text-2xl font-semibold">Portfolio</h1>
        </div>
      </nav>
    </div>
  );
};

export default Navbar;
